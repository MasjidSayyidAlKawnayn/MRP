You're running on a Windows device; use PowerShell.

Use TanStack Router for client-side routing; do not add React Router.

Always use shadcn-ui for UI components when possible.

Use Neon CLI with the alt account config directory by default:
`--config-dir C:\Users\ASUS\.config\neonctl-alt`
